def process_nav_includes(build_mode, data):
    """
    Process navigation data. Previously handled external repositories,
    now just returns data as-is since externals are not used.
    """
    return data
